export default class User {
  //constructor = oluşturucu
  constructor(id, firsName, lastName, city) {
    this.id = id;
    this.firsName = firsName;
    this.lastName = lastName;
    this.city = city;
  }
}
